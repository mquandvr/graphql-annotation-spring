spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa: 
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  http:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
      
  security:
    user:
      name: admin
      password: 123
      roles:
      - ADMIN
